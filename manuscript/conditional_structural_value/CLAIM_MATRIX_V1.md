# Conditional structural value — claim matrix v1

## Current empirical statements

| System | Supported statement |
|---|---|
| A-Islands | Structural configuration retained held-out incidence information conditional on pointwise climate support and nearest-source distance. |
| Tanzania | Adding the frozen geography-only structural feature worsened prediction relative to a matrix-aware current-flow + distance reference. |
| Azores eel | The sequentially refreshed augmentation improved macro log loss and won 5/5 held-out temporal units. |
| Louisiana King Rail | The sequentially refreshed augmentation was slightly favorable and won 7/8 held-out occasions. |
| Tampa seagrass | The static reused augmentation was strongly adverse and won 1/5 held-out folds. |

## Cross-system synthesis allowed now

The sign of structural added value is context dependent.

Two design properties are plausible moderators:
1. whether the reference already represents the same structural information;
2. whether the structural representation is refreshed at the response-relevant scale.

These are hypotheses, not identified causes.

## Prospective claim to earn

A future response-blind cross-system test may support:

> Structural information adds prediction chiefly when it is both nonredundant with the reference and aligned with the response-generating context.

Until that test closes, this sentence is a prediction, not a result.


## Known-truth causal claims now supported

| Claim | Frozen evidence |
|---|---|
| Structural added value is high when the reference omits the generating structural state and the representation is response-aligned. | weak+refreshed median Δlog loss = -0.17959; favorable 63/64. |
| Reference saturation causally reduces structural incremental value in the frozen generator. | strong-minus-weak moderation under refreshed representation: median +0.18797; positive 64/64. |
| Static reuse under dynamic truth causally reduces structural incremental value in the frozen generator. | static-minus-refreshed moderation under weak reference: median +0.22242; positive 64/64. |
| Weak-reference + refreshed is the best median cell. | Predeclared cell-ranking check passed. |

## Still prohibited

- assigning the A-Islands/Tanzania contrast causally to reference saturation;
- assigning the Azores/Louisiana/Tampa contrast causally to response alignment;
- treating synthetic effect magnitudes as estimates of any real ecological system;
- reopening or rescuing consumed endpoints;
- adding historical endpoints to a pooled common-effect meta-analysis.
