# Figure 3 accessibility description

The figure has five panels. Panel A shows a five-step outer-fold workflow from training-only resistance selection to untouched held-out scoring. Panel B contrasts the current-flow-plus-distance reference with the same model plus EOG connected frequency. Panel C shows one point for each of 60 species on a candidate-minus-reference log-loss axis; 17 points are below zero and 43 are above zero. Point size indicates the number of valid held-out predictions, which ranges from 12 to 14. Panel D shows four aggregate intervals. Both LOSO intervals are entirely above zero, including the primary estimate +0.032; both spatial-block intervals cross zero, including the primary estimate +0.011. Panel E reports 3,360 declared prediction rows, 826 matched LOSO rows per weighting, and 718 matched spatial-block rows per weighting, with all failure categories visible.
