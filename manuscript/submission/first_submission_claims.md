# Claims for the first submission — result-dependent template

The final first-submission claims cannot be fixed until the prospectively frozen A-Islands `C − R3` island-isolation adequacy test is executed. The original evidence below remains valid in either route.

## Frozen claim A — original A-Islands result

Among held-out islands matched within frozen pointwise-climate-support × nearest-training-source-distance strata, occurrence-conditioned connected frequency retained incidence-ordering information across 845 estimable plant taxa (mean conditional concordance 0.6177466; 95% species-bootstrap interval 0.6086806–0.6269445).

This claim does **not** yet establish information beyond a full multidimensional island-isolation reference.

## Frozen claim B — strong-reference Tanzania boundary

In the Tanzania forest-fragment benchmark, adding the generic EOG connected-frequency term after patch area, training-selected matrix-aware current flow, their interaction and nearest training source worsened primary LOSO prediction (+0.0321131 species-macro log loss; 95% interval +0.0174580 to +0.0486750). The geometry-only spatial-block sensitivity was smaller and uncertain.

## Pending claim C — island-isolation adequacy

The single authorized extension compares candidate C with final reference R3 on matched held-out log loss.

R3 already contains:

- local climate;
- recipient island area;
- continental-mainland distance;
- nearest focal-species training source;
- multi-source and area-weighted source pressure;
- nearest other island;
- surrounding island and surrounding landmass pressure;
- generic component exposure;
- generic mainland stepping-stone frequency.

C adds only the geography-only species-conditioned EOG connected-frequency term.

### If C − R3 is robustly negative

Permitted central claim:

> Species-conditioned occupied-source archipelago configuration retains held-out incidence information beyond a strong multidimensional generic island-isolation and source-pressure reference.

The first-submission route becomes *Journal of Biogeography*.

### If C − R3 is null, adverse or indeterminate

Permitted central claim:

> The original A-Islands signal beyond climate + nearest source is absorbed or not robustly extended once a much stronger multidimensional island-isolation reference is represented, illustrating how structural adequacy depends on reference content.

The first-submission route becomes *Ecological Informatics*.

## Methodological claim retained in either route

The paper contributes a leakage-safe, reference-conditioned validation architecture: explicit estimand separation, outer-training-only response-derived source features, predeclared graph-scenario aggregation, matched held-out comparison, explicit failures, strong-reference falsification and source-to-manuscript auditability.

## Claims deliberately not made

- first recognition that island isolation is multidimensional;
- first stepping-stone model;
- first graph representation of islands;
- first surrounding-landmass isolation metric;
- first source-conditioned or source-area-weighted connectivity model;
- first integration of suitability and connectivity;
- first biological validation of a graph connectivity model;
- new movement probability or colonisation model;
- universal predictive superiority.