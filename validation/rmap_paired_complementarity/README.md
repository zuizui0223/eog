# RMAP fresh paired complementarity attempt

This directory is a candidate-specific prospective attempt. It is not a new EOG
operator and must not be merged while outcome access is pending.

Final state: `ineligible_pre_response`.

The first workflow may read only the exact version-2 Figshare article JSON. It
must not dereference a file download URL. A passing inventory permits only the
next response-blind act: freeze separately identifiable registry/geometry and
code-documentation file IDs. If that separation requires inspecting a mixed
response archive, the attempt ends as `ineligible_pre_response`.

The final scientific endpoint, if every pre-response gate passes, is the frozen
paired comparison in `source_contract.json`: one strong learner with conventional
features versus the identical learner plus unchanged label-invariant Layer B.

The exact-version inventory passed, but exposed only a README, analysis code, and
six species-specific capture-history CSV files. No separately identifiable closed
site registry or coordinate geometry file exists. Opening the focal-species CSV to
recover its membership or geometry would violate the frozen separation gate, so no
file payload was downloaded and this attempt cannot proceed to outcome access.
