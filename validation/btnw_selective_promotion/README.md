# BTNW selective-promotion fresh candidate

This attempt tested only whether a Black-throated Green Warbler dynamic-occupancy dataset could satisfy the strengthened **pre-response** qualification gate for a future calibration-only selective Layer-B validation.

The source contract was frozen before any endpoint response payload access. It fixed the target, response semantics from independent documentation, a chronological 12/6/7-year train/validation/outer partition, the existing 30/30 and 10/10 class minima, exact author-repository geometry identity, and response-independent Haversine q20/q40/q60/q80 worlds.

Authoritative Stage0: run `34549868769`, job `103110310086`, artifact `10180371730`, ZIP SHA-256 `aa51c97fbc1bc26dd9e8e70bc33ffa1f901a4ac83c2b53623204ba68bd1adff3`.

Terminal state: `stop_pre_response_metadata_identity_transport_or_geometry`. The single authorized Dryad file-manifest metadata read did not expose the prospectively required immutable digest/digestType for `model_data.rds`. Response payload/header/rows/values remained zero, as did model fits, selector decisions, and heldout scores.

This is qualification/protocol evidence only. It does not count as predictive or empirical evidence and cannot be rescued by relaxing the response-file identity rule after observing the manifest. The closed EOG-WF synthesis is unchanged.
