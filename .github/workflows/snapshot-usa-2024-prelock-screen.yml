name: SNAPSHOT USA 2024 pre-lock source screen

on:
  push:
    branches:
      - agent/snapshot-usa-2024-prelock-screen
    paths:
      - validation/snapshot_usa_2024_prelock_screen/execute_once.json

permissions:
  contents: read

jobs:
  screen:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install package
        run: python -m pip install -e . --no-deps

      - name: Verify pre-lock authorization
        run: |
          python - <<'PY'
          import json
          import subprocess
          from pathlib import Path

          root = Path("validation/snapshot_usa_2024_prelock_screen")
          marker = json.loads((root / "execute_once.json").read_text())
          assert marker == {
              "screen_id": "snapshot_usa_2024_prelock_screen_v1",
              "execute_once": True,
              "authorization": "execution_authorization.json",
          }
          auth = json.loads((root / marker["authorization"]).read_text())
          bindings = {
              "source_screen_contract_git_blob_sha1": root / "source_screen_contract.json",
              "screen_script_git_blob_sha1": root / "screen.py",
              "source_transport_preflight_git_blob_sha1": Path(
                  "src/eog/v2/source_transport_preflight.py"
              ),
              "candidate_preflight_git_blob_sha1": Path(
                  "src/eog/v2/candidate_preflight.py"
              ),
          }
          for field, path in bindings.items():
              actual = subprocess.check_output(
                  ["git", "hash-object", str(path)], text=True
              ).strip()
              assert actual == auth[field], (field, actual, auth[field])
          PY

      - name: Run SNAPSHOT USA response-blind source screen
        run: python -m validation.snapshot_usa_2024_prelock_screen.screen

      - name: Verify response firewall
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          result = json.loads(
              Path(
                  "validation/snapshot_usa_2024_prelock_screen/"
                  "prelock_screen_result.json"
              ).read_text()
          )
          assert result["candidate_locked"] is False
          assert result["focal_species_selected"] is False
          assert result["response_file_requests"] == 0
          assert result["response_file_payload_bytes_opened"] == 0
          assert result["biological_response_values_opened"] is False
          assert result["model_fits"] == 0
          assert result["heldout_scores"] == 0
          assert result["counts_as_predictive_evidence"] is False

          print(json.dumps({
              "status": result["status"],
              "fingerprint": result["fingerprint"],
              "candidate_preflight": (
                  None if "candidate_preflight" not in result
                  else result["candidate_preflight"]["status"]
              ),
          }, sort_keys=True))
          PY

      - name: Upload pre-lock screen result
        uses: actions/upload-artifact@v4
        with:
          name: snapshot-usa-2024-prelock-screen
          path: validation/snapshot_usa_2024_prelock_screen/prelock_screen_result.json
          if-no-files-found: error
